# Master Miao V1.2.5 验证记录

验证日期：2026-09-04

测试源文件：`DB101P主草图.SLDPRT`（本机测试样件，未纳入公开仓库）

## 当前结论

Master Miao V1.2.5 已完成中文输入法安全改名、Release 构建、真实冷启动、改名逻辑自检和改名界面截图检查，并继承通过回归的三视图、双工作模式、去重组联动、项目保存/恢复、Excel 结构、STEP 同目录 / 独立镜像双目录与有效 STEP 归位能力。SolidWorks 宏和装配体批量导出调用顺序未改动，继续沿用 V1.1.2 已在真实用户桌面验收通过的流程。

本轮 V1.2.5 修改仅涉及列表名称编辑与对应测试，没有重新执行可见桌面 STEP 生成验收；此前后台 COM 隔离实例返回已知的 `SAVE_FAILED|1|0`，可见窗口又因 SolidWorks 与测试进程的 Windows 权限级别不同而无法取得自动化活动对象。本记录不把继承结果伪报为 V1.2.5 的完整桌面 STEP 成功结果。

## 构建与界面

- x64 WinForms Release 编译通过；程序文件为 `MasterMiao.exe`，主程序集名称为 `MasterMiao`，版本为 `1.2.5.0`。
- 原始品牌 PNG 已逐字节复制到项目，SHA-256 为 `E7459DBF45B1E42FAD89E04F3E8376C036637147C2774BE4B64FD4108A814679`。
- ICO 包含 9 个尺寸：16、20、24、32、40、48、64、128、256 像素；Windows 可从 EXE 正常提取 32×32 图标。
- 主窗口标题与红色页眉显示 `Master Miao / MASTER MIAO`，页眉和 Windows 窗口图标均使用用户提供的猫咪扳手图。
- 已复现并修复 V1.2.0 的启动异常：恢复检查不再于构造函数中调用 `BeginInvoke`，而是在 `Shown` 事件后执行。
- 未抑制启动提示的冷启动回归通过：构造阶段确认句柄尚未创建，显示后句柄正常创建，进程退出码为 0。
- 主界面 1540 × 920 截图通过：列表每行显示等轴测、前视和上视三张缩略图。
- 逐项模式 1240 × 820 截图通过：三张大图、名称、标签、输出位置、导出选择、上一个/下一个/跳过/定位及分类进度均可见。
- 英文主界面截图通过；固定控件、列表表头、状态和关键 SolidWorks 授权提示支持中英文切换。
- 列表支持多选、批量分类、80%–200% 缩放和 Ctrl + 滚轮。
- SolidWorks 定位代码使用源路径、实体序号和原实体名三项校验；跨源多选、未打开源文件和读取/导出进行中都会阻止定位。定位只改变选择高亮，不调用保存。
- 主页面“打开 SolidWorks”按钮在中英文界面均可见；按钮由用户主动触发，启动的会话按手动会话处理，不会在任务结束时自动关闭。
- STEP 存放选项在 1540×920 与 1100×720 界面完整可见；中英文均提供“同目录”和“独立镜像目录”两种选择及结构说明。

## V1.2.5 输入法安全改名回归

- 名称列保持只读，双击时在目标单元格上显示持久 `TextBox`；编辑框随滚动、列宽、行高与窗口尺寸重新定位。
- 逻辑自检逐字符输入中文名称，并在每个字符后模拟一次 Enter；编辑框持续可见，项目模型在输入阶段保持原值。
- “命名完毕 / Finish naming”提交后，净化后的名称写回表格、项目和重复件组；点击其他单元格也会提交。
- Enter 被显式消化，不触发 DataGridView 结束编辑；Esc 调用取消路径，不写回本次输入。
- 真实 WinForms 窗口生命周期下的改名逻辑自检退出码为 0；专用改名界面截图生成退出码为 0。
- 该改动不触及 `SolidWorksWorker`、`AssemblyStepExporter` 或 `macro\StepMacro.cs`。

## STEP 双目录归位回归

- 默认模式继续把 SLDPRT 与 STEP 放入同一分类根目录，旧项目缺少新字段时自动使用该兼容行为。
- 独立模式将 SLDPRT / SLDASM 路由到 `主输出\零件源文件`，将零件 STEP / 装配体 STEP 路由到 `主输出\STEP生产文件`。
- 使用两级分类 `结构件\板件` 和符合 `ISO-10303-21;` 文件头的模拟 STEP 执行真实文件归位：零件 STEP 进入镜像生产目录，零件源目录没有产生 STEP。
- 装配体 STEP 进入 `STEP生产文件` 根目录；Excel 继续读取响应中的实际 SLDPRT、STEP、SLDASM 与装配体 STEP 路径。
- “自动编号”同时检查零件源目录中的 SLDPRT 与生产目录中的 STEP；任一侧已存在时都会选择下一可用编号。
- 测试只在系统临时目录内创建隔离文件，结束后校验目标路径再清理；测试期间未启动 SolidWorks。

## SolidWorks 读取后保留与手动降级

- 使用 94 实体测试源执行真实读取，结果为 1/1 文件成功、94 个实体识别成功。
- 工作响应返回 `SolidWorksKeptOpen=True`、`RetainedSourceDocumentCount=1`；读取工作进程退出后，SolidWorks 窗口仍显示 `DB101P主草图 [只读]`。
- 工作进程在移交会话前逐一用源文件完整路径重新查询成功读取的文档；任一文档未保留会使读取任务失败，不会误报成功。
- 生命周期逻辑测试确认：只有成功的 scan 请求保留应用；失败源文件、export 请求、取消和异常不会进入保留分支。
- 自动启动前提供“由程序自动打开 / 我已手动打开 / 取消”三种选择；自动启动或自动化连接失败后进入手动启动与重试连接流程，并复用原读取请求继续执行。
- 手动连接要求恰好一个 SolidWorks 进程；该会话不会被标记为程序所有，也不会被程序关闭。

## 去重与多文件逻辑

- 自动逻辑测试构造两个相同几何实体，启用去重后显示项数减少 1。
- 修改代表项的导出名称、分类和导出选择后，同组成员同步更新。
- 去重分组以整个项目为范围，不局限于当前源文件筛选。
- 多源项目保留“全部文件”视图，列表 Ctrl/Shift 多选和批量分类可跨源使用。

## 项目保存与恢复

- 用 94 实体样例执行项目自检，成功生成 Schema 2 的 `project.swbody.json`。
- 项目 `Previews` 中生成 282 张 PNG，即 94 个实体 × 等轴测/前视/上视三张。
- 项目记录保存源文件引用、名称/标签、目录树、输出路径、导出设置、缩放、逐项索引、保存时间和导出状态。
- 尚未指定保存位置时，自动恢复记录写入 `Data\Recovery`；指定项目位置后自动保存到项目文件夹并清理临时恢复记录。
- 打开项目时检查源文件是否丢失或发生大小/修改时间变化；丢失文件可重新绑定，变化文件提示重新读取。
- 关闭窗口始终确认。任务进行中先发送安全取消；未完成工作提供保存关闭、不保存关闭和取消三种路径。

## Excel 报表

- 自检工作簿包含 1 个工作表、15 列和 1 条零件记录。
- 该记录包含 3 个独立图片对象，分别锚定在 A、B、C 列，对应等轴测、前视和上视。
- 关键范围为 `A1:O3`；数量列为数值，表头冻结并设置筛选。
- 公式错误扫描匹配 0 项，工作表结构和渲染检查通过。
- 中文与英文表头均由同一写入器生成；英文模式会转换常用导出状态。

## SolidWorks 拆分与装配体回归

- 历史完整扫描识别 94 个实体；94/94 均已有完整三视图。
- 本轮三实体回归：3/3 个 SLDPRT 保存成功，3/3 重新只读打开并确认恰好一个实体。
- 本轮原位装配体：1/1 个 SLDASM 成功，包含 3 个按原坐标固定的组件。
- 失败的 STEP 宏日志为 `ITEM|0|SAVE_FAILED|1|0`、`RESTORED|True`、`DONE`；SolidWorks 选项已恢复，失败 STEP 未提交。
- 本轮两个 SolidWorks 测试进程均已结束；工作后 `SLDWORKS` 进程数为 0。

## 已继承的真实桌面 STEP 验收

- V1.1.2 正式构建在用户真实桌面由授权弹窗启动可见 SolidWorks，全程无需手动宏。
- 3/3 个零件 STEP、1/1 个装配体 STEP 成功；4/4 个文件均通过 `ISO-10303-21;` 文件头检查。
- 编译型宏日志为 `ITEM|0|OK|0|0`、`RESTORED|True`、`DONE`；任务后 SolidWorks 自动关闭。
- 同一方法还曾对 94 组件装配体生成 94 个零件 STEP 和 1 个装配体 STEP，95/95 个文件有效。
- V1.2.4 只修改通过格式校验后的正式文件目标路径；V1.2.5 没有修改 `macro\StepMacro.cs` 或工作进程的 STEP 调用顺序。

## 原始文件保护

- 预期基线大小：15,839,319 字节。
- 预期 UTC 修改时间：2026-09-03 08:39:18。
- 预期 SHA-256：`6BCFA7DBF75148AD16267DCC11891F09C4F2703A0EEE0D36A1A1981F69E43279`。
- 测试只读取原始 SLDPRT；输出全部位于工作区测试目录。
- 没有修改系统配置或注册表。

用户桌面既有测试目录保持不动。V1.1.2 正式发行包保持不动。
