# Master Miao（原 SW BODY ORGANIZER）V1.2.0 需求基线

状态：需求整理阶段

## 1. 三视图缩略图

- 每个实体保留并展示等轴测、前视和上视三张缩略图。
- 列表模式使用三图组合缩略图；单件模式显示三张大图。
- Excel 报表分别包含三张缩略图，不再只输出一张等轴测图。

## 2. 双分类模式

- 列表模式：同时查看多个实体，支持批量选择、重命名和分类。
- 单件向导模式：每页只显示一个实体及其三张大图、名称、分类、来源、重复数量和状态。
- 单件模式提供“上一个”“下一个”“跳过”和“在 SolidWorks 中定位”。
- 两种模式共用同一份项目数据，切换模式不会丢失修改。
- 实时显示总实体数、已分类数、未分类数和当前序号。

## 3. SolidWorks 实体定位

- 当对应多实体主文件已经在 SolidWorks 中打开时，可从列表或单件模式请求定位实体。
- 程序核对来源文件、实体索引、原名称和几何指纹后，在 SolidWorks 中选择并高亮实体。
- 列表模式允许对同一来源文件的多个实体进行高亮。
- 不保存、不修改源文件；读取或导出任务进行中禁用定位功能。
- 来源文件没有打开、活动文件不一致或无法唯一定位时，显示明确原因，不自动修改用户会话。

## 4. 列表缩放

- 支持 80%–200% 缩放，联动字体、行高、表头和缩略图尺寸。
- 支持缩放控件及 `Ctrl + 鼠标滚轮`。
- 缩放比例保存为用户设置，下次启动继续使用。

## 5. 多文件统一分类

- 一次选择或拖入多个 SLDPRT。
- 支持跨来源多选实体并批量指定分类标签。
- 支持查看全部文件或按来源文件筛选。
- 单件模式可连续浏览多个文件中的实体，并清楚显示当前来源。
- 导出前对所有来源执行全局重名、路径和源文件变化检查。

## 6. 几何去重视图

- 启用“相同几何仅导出一件”后，相同几何实体在列表和单件模式中折叠为一条代表记录。
- 代表记录显示合并数量、全部来源和出现位置；原始记录仅隐藏，不删除。
- 取消选项后立即恢复全部实体。
- 对代表记录的名称、分类和导出选择应用于整个重复组。
- 几何去重默认跨本项目全部导入文件生效。
- 去重模式与原位装配体冲突时继续在导出前阻止，并说明位置保持方面的原因。

## 7. 输出位置

- 列表和单件模式始终显示当前主输出路径及“选择”“打开目录”按钮。
- 支持选择任意用户有权限的文件夹。
- 保存最近使用路径和项目指定路径。
- 导出前验证目标路径、写入权限、路径长度和剩余空间；失败时不启动 SolidWorks。
- STEP 提供“与 SLDPRT 同目录”和“独立双目录（镜像分类树）”两种存放模式。
- 独立双目录模式在主输出下建立 `零件源文件` 与 `STEP生产文件`；两者使用相同的分类父子结构，SLDPRT/SLDASM 与 STEP 文件互不混放。
- Excel 报表记录两套目录中的实际文件路径；切换 STEP 存放模式不会改变实体名称、标签或分类关系。

## 8. 中英文语言

- 第一次启动在主窗口出现前选择“简体中文”或“English”。
- 提供“下次不再询问”；选中后记住语言。
- 设置页可随时切换语言，并可恢复启动询问。
- 设置保存在程序本地配置文件中，不写注册表。
- 系统文字、弹窗、状态和 Excel 表头参与本地化；用户名称、标签和路径不自动翻译。

## 9. 关闭确认

- 用户点击关闭按钮时始终显示任务状态和关闭确认。
- 没有项目时显示普通“确认关闭软件”。
- 项目已完成时显示已分类数量、导出状态，并询问是否确认关闭。
- 项目未完成或存在未保存修改时显示总数、已分类数、未分类数和上次保存时间，并提供：
  - 保存并关闭；
  - 不保存直接关闭；
  - 取消并返回程序。
- 读取或导出正在进行时不能立即强制退出；提供“安全取消并在任务结束后关闭”和“继续等待”。
- 程序自行启动的 SolidWorks 必须在安全取消完成后按 PID 与启动时间核验回收；用户原有 SolidWorks 不退出。

## 10. 未完成项目保存与恢复

- 用户可以为当前工作选择一个保存文件夹和项目名称。
- 项目保存内容包括：
  - 所有源文件引用及完整性信息；
  - 实体名称、分类标签和文件夹树；
  - 三视图缩略图；
  - 导出选择、去重选项和输出路径；
  - 列表筛选、缩放比例、当前单件序号和分类进度；
  - 最近一次导出结果与失败信息。
- 采用一个独立工作项目文件夹，内部保存 `project.swbody.json` 和 `Previews`，不复制或改写原始 SLDPRT。
- 手动“保存项目”“项目另存为”均允许选择目标文件夹。
- 修改名称、分类或选择状态后执行延迟自动保存；自动保存失败只提醒，不阻塞继续操作。
- 下次启动可通过“最近项目”或“打开项目”继续分类。
- 启动时检测未正常关闭留下的自动保存，并询问是否恢复。
- 如果源文件被移动，允许用户重新指定位置；如果大小、时间或指纹变化，要求重新读取后再导出。

## 完成状态定义

- “已分类”：实体或去重代表组的分类不是“未分类”，且导出名称有效。
- “分类完成”：当前需要处理的全部实体均已分类。
- “导出完成”：当前选中的全部代表项均有最近一次成功导出记录，且项目自该次导出后没有影响输出的修改。
- 关闭弹窗同时显示分类完成状态和导出完成状态，不替用户自动判断是否应该关闭。

## 验收重点

- 列表与单件模式反复切换后数据一致。
- 关闭、自动保存、恢复和源文件重新绑定不会丢失名称或分类。
- 两个以上源文件可以跨文件批量分类和全局去重。
- 三视图在界面、保存项目和 Excel 中均完整。
- SolidWorks 高亮不修改源文件，且不干扰读取/导出会话。
- 中英文首次选择、记忆、重新询问和报表本地化均通过。
- 所有失败分支给出具体原因，并保持 V1.1.2 的源文件只读、暂存验证、会话恢复和 STEP 选项恢复机制。
