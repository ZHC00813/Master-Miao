# V1.2.5 兼容性审计 / V1.2.5 compatibility audit

R2 补充 / R2 addendum: 原“人工确认后折叠”方案已根据用户反馈撤回，恢复勾选即折叠、取消即还原，并在正式导出前核对每个合并成员。定位与导出安全检查分离。The manual-confirmation gate below is historical: R2 restores immediate reversible checkbox folding and checks all members before export. Locating uses live identity independently of strict export validation. See [R2_FIX_VALIDATION.md](R2_FIX_VALIDATION.md).

## 中文

### 基线和目标

本轮逐文件读取用户保存的 `Master-Miao-v1.2.5.zip`，以其中 9 个 C# 源文件和实际界面行为作为兼容基线。V1.2.6 保留 V1.2.5 的导入、列表、逐项整理、三视图、分类、输出选择和 SolidWorks 使用流程；新增的存储、完整性和界面工作流代码只用于加固原流程，不改变用户必须完成的主要步骤。

### 已定位的回归

| 区域 | V1.2.5 行为 | 原 V1.2.6 问题 | 当前处理 |
|---|---|---|---|
| 读取 | 打开文件后直接枚举实体 | 把导出阶段的未保存/内存严格检查提前到读取；SolidWorks 自动重建也会被当作读取失败 | 读取恢复宽容策略；记录警告并允许查看分类，正式导出仍严格核对磁盘摘要、配置和内存状态 |
| 大文件性能 | 每个实体只做本次读取工作 | 把历史实体发给工作进程并逐实体遍历历史持久引用，94 体时形成近似 O(n²) COM 调用 | 工作进程只接收源文件身份；扫描后在界面进程按索引和几何指纹 O(n) 恢复用户编辑 |
| 几何取证 | 每体生成一次主要几何指纹 | 新版增加位置相关的实体证据 | 保留独立几何证据，以兼容定位和 STEP 重导入校验；性能优化集中于移除历史实体的嵌套匹配 |
| 逐项整理 | 名称/分类应在返回列表后保留 | 模态窗口运行时，自动保存可能从陈旧列表行反写模型，覆盖刚保存的逐项修改 | 列表成为只读投影；逐项名称、分类和勾选一次提交到唯一模型，再刷新列表和保存快照 |
| 列表改名 | 中文输入法应连续选字 | Enter 可能被表格或窗体解释为结束编辑；行刷新也可能让行号指向失效 | 使用独立的输入法安全编辑框；Enter 归编辑框所有但不提交，单击“命名完毕”或离开单元格提交；编辑对象按实体引用而非易变行号保存 |
| 字体 | 原布局可用但部分灰字偏小 | 次要文字对比度不足 | 主字体 9.75pt，次要字体至少 9pt，并加深文字颜色；1540×920 与 1100×720 均保留原布局 |

### 不回退的安全边界

- 扫描允许 SolidWorks 自动重建产生的保存标志，但只作为“可以查看和分类”的结果；导出前仍要求源文件摘要、配置、持久实体身份和内存几何一致。
- 几何指纹相同只表示“疑似重复”，不会自动折叠。只有人工确认的重复组才共享名称、分类和导出选择。
- 读取后主文件继续留在 SolidWorks，便于定位；程序不会保存用户源文件，也不会为解决连接问题修改注册表、权限或宏安全设置。

### 当前自动证据

- 真实 WinForms 控件连续编辑：已保存/未保存项目各 11.2 秒、跨 4 次自动保存周期；Enter 保留草稿，“命名完毕”提交，Esc 取消。
- 逐项窗口通过实际“保存并下一个”和“上一个”按钮，跨自动保存后名称、分类及项目 JSON 一致。
- 读取兼容逻辑自检通过；失败或未访问的源文件不覆盖原分类；重新读取按 O(n) 恢复未变化实体的名称、分类和勾选。
- 项目存储 25 项、STEP 文件夹/输出规划、Excel 结构、启动及中英界面截图回归通过。

真实中文输入法候选窗以及修改后读取路径的 SolidWorks 实机复测仍需要在不占用用户当前工作会话时进行，因此本文件不把它们标成生产验收完成。

## English

### Baseline and objective

This pass compares every source file in the user-supplied `Master-Miao-v1.2.5.zip`. Its nine C# files and visible workflow are the compatibility baseline. V1.2.6 retains the V1.2.5 import, list, guided organizer, three-view preview, categorization, destination selection and SolidWorks workflow. Focused persistence, integrity and UI helpers harden that workflow without adding required user steps.

### Regressions and resolutions

| Area | V1.2.5 behavior | Earlier V1.2.6 regression | Current resolution |
|---|---|---|---|
| Scan | Open and enumerate bodies | Export-only dirty-memory checks ran during scan, so an automatic SolidWorks rebuild could reject a readable part | Scan records a warning and permits organization; export still requires matching digest, configuration, identity and memory state |
| Large files | One scan pass per body | Prior bodies were sent to the worker and persistent references were compared through nested COM loops, approaching O(n²) on 94 bodies | The worker receives source identity only; an O(n) UI pass restores edits after scanning |
| Geometry evidence | One primary fingerprint | Position-sensitive evidence was added | Retain separate evidence for locating and STEP reimport verification; optimize the nested prior-body matching instead |
| Guided edits | Saved values should return to the list | Autosave could replay stale grid rows over newly committed guided values | The grid is a projection only; name, category and selection commit together to one model before refresh and snapshot |
| List rename | IME composition should continue | Enter could escape to the grid/dialog and row refresh could invalidate a row index | A dedicated IME-safe editor owns Enter without committing; explicit finish/click-away commits by stable body reference |
| Readability | Existing layout | Small low-contrast secondary text | 9.75pt base, 9pt minimum secondary text, darker colors, with the original 1540×920 and 1100×720 layouts retained |

### Safety retained

- A dirty flag caused by opening/rebuilding no longer blocks inspection, but export remains fail-closed on source digest, configuration, persistent identity and in-memory geometry.
- A matching fingerprint is only a possible duplicate. Only a user-confirmed group shares editing/export state.
- Scanned source documents remain open for locating bodies. The application never saves source CAD or changes registry, privilege or macro-security settings.

### Current automated evidence

- Real WinForms controls retain list drafts for 11.2 seconds in saved and unsaved projects across four autosave periods; Enter keeps the draft, Finish commits and Esc cancels.
- Actual guided Save-and-next and Previous button clicks retain the name, category and saved JSON across autosave.
- Scan compatibility, partial failure preservation and O(n) edit restoration self-tests pass.
- Twenty-five persistence checks plus STEP path/planning, report structure, startup and bilingual layout tests pass.

Native IME candidate-window behavior and the revised SolidWorks scan path still require a same-privilege desktop retest after the user's active production session is free. They are not marked as production acceptance here.
