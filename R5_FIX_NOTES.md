# R5 修复与验证

## 修复内容

- 增加“保存源文件并重读”和“重新关联”，避免另存或重读后必须从头命名。
- 按持久引用、原实体名和几何证据恢复可唯一对应的实体编辑，避免依赖实体顺序。重读前备份项目与预览。
- 在明确确认保存后，处理实体读取及预览过程中出现的待保存状态，保存后重新核对几何、配置和文件内容身份。
- 检查完整文件路径，明确区分同名文档冲突、只读和其他会话占用。
- 避免切换文档时请求自动重建，保留导出前的保存状态及几何检查。
- STEP 回读使用唯一命名的参照副本，避免与导入文档同名。
- 原位几何验证增加双向空布尔差集检查，处理对称体重合变换歧义及质量积分差异；不通过移动实体来强行匹配。
- STEP 校验使用 3D Interconnect、有效模板和临时导入设置，并在结束后恢复相关设置。
- 装配 STEP 主文件包含完整几何，避免依赖拆分文件才能读取。

## 已完成的实测

环境：Windows x64、SolidWorks 2024 SP0.1；使用一个七实体零件的独立副本。

- 保存、重新关联并重读后，全部七个已有命名保留，扫描结束后没有未保存标记。
- 实体顺序或引用变化后的命名恢复通过；几何变化和身份歧义不会错误继承名称。
- 七个 SLDPRT、七个独立 STEP、一个原位 SLDASM、一个装配 STEP 导出成功，并通过相应几何与位置检查。最终批次无需手动选择模板。
- 平移 1 mm 的实体及等体积镜像后的实际非对称零件均被拒绝。
- 导入测试前后六项相关 SolidWorks 设置一致。
- 修复版编译与项目逻辑自检通过。

本次结果适用于所测试的模型与环境；未声称所有历史测试、任意零件或其他 SolidWorks 版本均已通过验证。
